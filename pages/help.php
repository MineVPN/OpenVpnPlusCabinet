<?php
/**
 * OVPNPlus — страница «Инструкция».
 *
 * Пишется для человека, который купил VPS и второй впн, но не обязан
 * знать, что такое policy routing. Поэтому здесь нет команд, которые
 * нужно куда-то вводить, — только объяснения и порядок действий.
 */

require_once __DIR__ . '/../includes/ovp_helpers.php';

ovp_require_auth();

$net        = ovp_server_net();
$hasConfig  = file_exists(OVP_UP_CONF) && filesize(OVP_UP_CONF) > 0;
$killswitch = ovp_killswitch_on();
?>

<div class="page-head">
  <div class="page-head__title"><h1>Инструкция</h1></div>
  <p class="page-head__note">
    Как это устроено и что делать в типичных ситуациях.
  </p>
</div>

<div class="stack">

  <div class="card">
    <div class="card__head"><h2 class="card__title">Что делает этот сервер</h2></div>

    <p class="card__lead">
      Ваши устройства подключаются не напрямую к купленному впн, а к этому серверу.
      Он принимает их и уже от себя отправляет трафик дальше — во второй впн.
      Получается цепочка из двух звеньев.
    </p>

    <div class="chain">
      <span class="chain__node">Ваши устройства</span>
      <span class="chain__arrow">→</span>
      <span class="chain__node chain__node--self">Этот сервер</span>
      <span class="chain__arrow">→</span>
      <span class="chain__node">Второй впн</span>
      <span class="chain__arrow">→</span>
      <span class="chain__node">Интернет</span>
    </div>

    <p class="card__hint">
      Зачем так: второй впн видит только адрес этого сервера, а не адреса ваших
      устройств. А вы можете менять второй впн когда угодно, не переустанавливая
      конфиги на телефонах и ноутбуках — они всегда подключены к одному и тому же
      адресу.
    </p>
  </div>

  <div class="card">
    <div class="card__head"><h2 class="card__title">Порядок действий</h2></div>

    <ol class="steps">
      <li>
        <span class="steps__t">Раздайте конфиги устройствам</span>
        Файлы <span class="data">.ovpn</span> лежат на сервере в папке
        <span class="data">/root</span>. Их создал установщик — по одному на устройство.
        Откройте такой файл в приложении OpenVPN на телефоне или компьютере.
      </li>
      <li>
        <span class="steps__t">Проверьте, что интернет работает</span>
        Пока второй впн не загружен, сервер работает как обычный OpenVPN:
        устройства выходят в интернет с адреса самого сервера. Это нормальное
        рабочее состояние, а не ошибка.
      </li>
      <li>
        <span class="steps__t">Загрузите конфиг второго впн</span>
        Вкладка «Подключение» — перетащите файл <span class="data">.ovpn</span>,
        полученный от продавца второго впн. Если он требует логин и пароль,
        заполните поля под окном загрузки.
      </li>
      <li>
        <span class="steps__t">Готово</span>
        Дальше сервер следит за туннелем сам: проверяет связь каждые две секунды
        и переподключается при обрыве. Заходить в панель для этого не нужно.
      </li>
    </ol>
  </div>

  <div class="card">
    <div class="card__head">
      <h2 class="card__title">Если второй впн упадёт</h2>
      <span class="badge badge--<?= $killswitch ? 'warn' : 'off' ?>">
        <?= $killswitch ? 'Интернет отключится' : 'Работает напрямую' ?>
      </span>
    </div>

    <p class="card__lead">
      Сейчас выбран режим
      <?php if ($killswitch): ?>
        <b>Kill Switch</b>: если второй впн станет недоступен, интернет у ваших
        устройств пропадёт полностью. Ни один запрос не уйдёт с адреса этого сервера.
      <?php else: ?>
        <b>работать напрямую</b>: если второй впн станет недоступен, устройства
        продолжат выходить в интернет через этот сервер. Связь не прервётся,
        но сайты в этот момент увидят страну этого сервера.
      <?php endif; ?>
      Поменять можно на вкладке «Подключение».
    </p>

    <p class="card__hint">
      Переключение происходит автоматически и в обе стороны: когда второй впн
      вернётся, трафик сам пойдёт обратно через него.
    </p>
  </div>

  <div class="card">
    <div class="card__head"><h2 class="card__title">Обход VPN</h2></div>

    <p class="card__lead">
      Иногда часть трафика удобно пустить мимо второго впн — например, телефонию,
      которой важна минимальная задержка, или сервис, блокирующий адреса впн.
      Добавьте такие адреса на вкладке «Обход VPN».
    </p>

    <p class="card__hint">
      Пять адресов добавить нельзя: <span class="data">8.8.8.8</span>,
      <span class="data">8.8.4.4</span>, <span class="data">1.1.1.1</span>,
      <span class="data">1.0.0.1</span>, <span class="data">9.9.9.9</span>.
      Ими сервер проверяет, жив ли туннель. Если пустить их напрямую, проверка
      станет успешной всегда — и падения второго впн перестанут замечаться.
      Для DNS возьмите другой адрес, например <span class="data">77.88.8.8</span>.
    </p>
  </div>

  <div class="card">
    <div class="card__head"><h2 class="card__title">Частые вопросы</h2></div>

    <div class="qa">
      <div class="qa__q">Загрузил конфиг, а пишет «Нет связи»</div>
      <div class="qa__a">
        Откройте вкладку «События» — там будет причина. Чаще всего это неверный
        логин и пароль ко второму впн либо закончившаяся у продавца подписка.
        Сервер продолжит попытки подключения сам.
      </div>

      <div class="qa__q">Конфиг не принимается: «адрес пересекается с подсетью»</div>
      <div class="qa__a">
        Второй впн выдаёт адреса из той же сети, что и ваши клиенты
        (<span class="data"><?= htmlspecialchars($net['cidr']) ?></span>).
        Два одинаковых маршрута в системе — и клиенты перестанут быть доступны
        после ближайшей перезагрузки. Попросите у продавца конфиг с другой
        адресацией.
      </div>

      <div class="qa__q">Устройства подключаются, но интернета нет</div>
      <div class="qa__a">
        Проверьте вкладку «Пинг» в режиме «напрямую с сервера». Если и там
        потери — проблема в канале самого сервера, а не во втором впн.
      </div>

      <div class="qa__q">Хочу сменить второй впн</div>
      <div class="qa__a">
        Просто загрузите новый файл на вкладке «Подключение». Старый заменится,
        конфиги на устройствах менять не нужно. Если новый не поднимется,
        сервер сам вернёт предыдущий рабочий.
      </div>

      <div class="qa__q">Потерял пароль от панели</div>
      <div class="qa__a">
        Он записан на сервере в файле <span class="data">/root/lk.txt</span>
        вместе с адресом панели.
      </div>

      <div class="qa__q">Нужно добавить ещё устройства</div>
      <div class="qa__a">
        Запустите установщик заново с нужным количеством конфигов. Учтите:
        ключи при этом создаются новые, поэтому старые конфиги перестанут
        работать и раздать придётся все заново.
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card__head"><h2 class="card__title">Текущие параметры</h2></div>
    <div class="facts facts--grid">
      <div class="fact">
        <span class="fact__k">Подсеть клиентов</span>
        <span class="fact__v"><?= htmlspecialchars($net['cidr']) ?></span>
      </div>
      <div class="fact">
        <span class="fact__k">Адрес сервера в туннеле</span>
        <span class="fact__v"><?= htmlspecialchars($net['gw']) ?></span>
      </div>
      <div class="fact">
        <span class="fact__k">Второй впн</span>
        <span class="fact__v"><?= $hasConfig ? 'загружен' : 'не загружен' ?></span>
      </div>
      <div class="fact">
        <span class="fact__k">При падении второго впн</span>
        <span class="fact__v"><?= $killswitch ? 'отключать интернет' : 'работать напрямую' ?></span>
      </div>
    </div>
  </div>
</div>
